function [ list ] = createDictionary(filename, wordLen)
file = fopen(filename, 'r');
place = 1;
list = [];
while ~feof(file)
    line = fgetl(file);
    if(length(line) == wordLen)
        list{place} = line;
        place=place+1;
    end
end
fclose(file);
end


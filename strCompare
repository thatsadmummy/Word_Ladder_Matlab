function [output] = strCompare(str1, str2)
    for i=1:length(str1)
        if(str1(i)<str2(i))
            output =  -1;
            return
        end
        if(str1(i)>str2(i))
            output =  1;
            return
        end
        
    end
    output =  0;
    return
end
